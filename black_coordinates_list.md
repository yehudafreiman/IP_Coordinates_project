# Black Coordinates List

## 1. רקע מבצעי

בשנים האחרונות מתבצעת פעילות סייבר עוינת מתמשכת נגד גופים ממשלתיים, תשתיות רגישות (כגון הרכבת הקלה בגוש דן) ואישים בעלי חשיבות. פעילות זו מתבצעת באמצעות תשתיות מבוזרות, שימוש בכתובות IP מתחלפות והסתרת מקורות הפעולה מאחורי שכבות טכנולוגיות שונות.

היקף הפעילות, התדירות והנזק הפוטנציאלי הובילו להבנה כי התמודדות עם האיום אינה יכולה להישאר ברמה הדיגיטלית בלבד. נדרש חיבור עמוק יותר בין מידע טכנולוגי לבין המציאות הפיזית שמאחוריו.

במוסד התקבלה החלטה לפתח יכולת תשתיתית שתאפשר ראייה רחבה יותר של הזירה --- כזו שתשמש בסיס לפעילות מודיעינית, חקירתית ומבצעית בהמשך.

בשלב זה, לאחר שהוגדר הצורך המבצעי והובהרה חשיבותו, אתם נקראתם למשימה.

עליכם להקים את **Black Coordinates List** --- מערכת תשתיתית שתיבנה בצורה מדויקת, יציבה וניתנת להרחבה, ותשמש רכיב ליבה במערך גדול יותר של מערכות ויכולות.

הדגש בפרויקט זה אינו על תחכום או אלגוריתמיקה, אלא על בנייה נכונה של תשתית: הבנה של זרימת מידע, הפרדת אחריות, ויכולת לסמוך על המערכת כבסיס להמשך פעילות.

---

## 2. מה המערכת עושה

מערכת **Black Coordinates List** היא מערכת תשתיתית שמטרתה לבצע את הפעולות הבאות:

- קבלת כתובות IP הקשורות לפעילות סייבר
- המרת כתובות IP לנקודות ציון גאוגרפיות באמצעות שירות חיצוני
- העברת נקודות הציון בין שירותים פנימיים במערכת
- אחסון נקודות הציון במאגר נתונים מהיר מבוסס Key-Value
- שליפה מרוכזת של כלל נקודות הציון שנשמרו במערכת

המערכת פועלת כ־**שכבת ליבה תשתיתית**, ומספקת נתונים לשימוש עתידי על ידי מערכות, שירותים או גורמים אחרים בארגון.

---

## 3. ארכיטקטורה כללית של המערכת

מערכת **Black Coordinates List** בנויה כמערכת תשתיתית מבוססת שירותים (Services), שבה כל רכיב אחראי על תחום מוגדר וברור. המערכת עושה שימוש בתקשורת HTTP בין שירותים, בשירות חיצוני להמרת IP למיקום גאוגרפי, ובמסד נתונים מהיר מסוג Key-Value לצורך אחסון ושליפה.

### הטכנולוגיות המרכזיות במערכת:

- Python + FastAPI עבור שירותי ה־Backend
- HTTP (REST) לתקשורת בין רכיבים
- שירות חיצוני להמרת IP לנקודות ציון
- Redis כמאגר נתונים תשתיתי מהיר
- עבודה בסביבה מבודדת (Container-based)

הארכיטקטורה מתוכננת כך שכל רכיב ניתן להבנה, לבדיקה ולהחלפה, מבלי להשפיע על שאר המערכת.

### רכיבי המערכת

המערכת מורכבת משלושה רכיבים מרכזיים:

1. **שירות קליטת IP והמרה למיקום (Service A)**
2. **שירות אחסון ושליפה של נקודות ציון (Service B)**
3. **מאגר נתונים Redis**

להלן פירוט אחריות מדויק לכל רכיב.

---

### 3.1 שירות A -- IP Resolution Service

**תפקיד במערכת**

שירות זה מהווה את נקודת הכניסה למידע חיצוני. הוא אחראי לקבל כתובות IP, להמיר אותן למיקומים גאוגרפיים, ולהעביר את הנתונים הלאה לשירות פנימי.

**מה השירות עושה**

- חושף API לקבלת כתובת IP
- שולח בקשה לשירות חיצוני להמרת IP למיקום
- מקבל ומעבד את התשובה של השירות החיצוני (נקודות ציון)
- שולח את נקודות הציון לשירות האחסון (**שירות B** עליו מפורט בהמשך)

**מה השירות לא עושה**

- אינו שומר נתונים
- אינו עובד ישירות מול Redis
- אינו מבצע שליפות של מידע
- אינו מקבל החלטות או מנתח נתונים
- אינו אחראי על הצגת מידע

> זהו שירות מתאם (Adapter) בין העולם החיצוני לבין המערכת הפנימית.

---

### 3.2 שירות B -- Coordinates Storage Service

**תפקיד במערכת**

שירות זה מהווה את שכבת האחסון והשליפה של המערכת. הוא אחראי לקבל נקודות ציון, לשמור אותן בצורה מסודרת, ולהחזיר אותן לפי דרישה.

**מה השירות עושה**

- חושף API פנימי לקבלת נקודות ציון (**משירות A**)
- מבצע ולידציה בסיסית לנתונים הנכנסים באמצעות pydantic
- שומר את הנתונים ב־Redis
- מאפשר שליפה מרוכזת של כלל הנתונים שנשמרו

**מה השירות לא עושה**

- אינו פונה לשירותים חיצוניים
- אינו יודע מה מקור הנתונים (מאיזה שרת הגיעו, משתמש, מערכת)
- אינו מבצע חישובים או ניתוחים

> זהו שירות תשתיתי "שקט", שמטרתו אחת: אחסון ושליפה אמינים.

---

### 3.3 Redis -- מאגר הנתונים

**תפקיד במערכת**

Redis משמש כמאגר נתונים מהיר לאחסון נקודות הציון.

**מאפיינים מרכזיים**

- מבוסס Key-Value
- In-Memory עם Persistency בסיסי
- גישה מהירה ופשוטה
- מתאים לאחסון מבנים פשוטים של נתונים

**אופן השימוש בפרויקט**

- שמירת נקודות ציון כמבני נתונים פשוטים
- שליפה כללית של כל הערכים
- ללא שימוש ב־TTL דינמי
- ללא לוגיקה עסקית בתוך מסד הנתונים

---

### עיקרון ארכיטקטוני מנחה

כל רכיב במערכת:

- יודע רק את המינימום הנדרש
- אינו תלוי בלוגיקה פנימית של רכיבים אחרים

מבנה זה מאפשר:

- פשטות
- יכולת בדיקה
- תחזוקה קלה
- הרחבה עתידית ללא שבירת המערכת

---

## 4. זרימת מידע מלאה של המערכת

### זרימה ראשית: קליטת IP ואחסון נקודות ציון

1. גורם חיצוני (סוכן, מערכת או כלי בדיקה) שולח בקשת POST ל־Service A עם כתובת IP.
2. Service A מקבל את הבקשה ומתחיל תהליך טיפול בכתובת ה־IP.
3. Service A שולח בקשת HTTP לשירות חיצוני להמרת IP למיקום גאוגרפי.
4. השירות החיצוני מחזיר ל־Service A נתונים הכוללים נקודות ציון גאוגרפיות.
5. Service A מבצע עיבוד בסיסי לנתונים שהתקבלו מהשירות החיצוני, ומחלץ את המידע הרלוונטי בלבד.
6. לאחר קבלת נתונים תקינים, Service A שולח בקשת POST פנימית ל־Service B עם נקודות הציון.
7. Service B מקבל את הבקשה ומבצע ולידציה בסיסית לנתונים.
8. Service B שומר את נקודות הציון במאגר הנתונים Redis.
9. התהליך מסתיים לאחר שהנתונים נשמרו בהצלחה במאגר.

### זרימה משנית: שליפת נתונים

1. גורם פנימי (כלי בדיקה, מערכת עתידית או שירות נוסף) שולח בקשת GET ל־Service B.
2. Service B מקבל את הבקשה ומבצע שליפה של כלל נקודות הציון ממאגר הנתונים.
3. Redis מחזיר את הנתונים שנשמרו.
4. Service B מחזיר את רשימת נקודות הציון, בהתאמה לכתובת ה-IP הרלוונטית, לגורם המבקש.

### דגשים חשובים לזרימת המידע

- כל זרימה מתחילה בבקשת HTTP מפורשת.
- Service A אינו שומר נתונים ואינו ניגש ל־Redis.
- Service B אינו פונה לשירותים חיצוניים ואינו יודע מה מקור הנתונים.
- כל תקשורת בין רכיבים היא מפורשת (HTTP) וניתנת למעקב.
- Redis נגיש רק דרך Service B.

---

## 5. מבנה נתונים פנימיים

### מידע שנשמר במערכת

המערכת תאחסן מידע על נקודות ציון גאוגרפיות המקושרות לכתובת IP.

**דוגמה למבנה נתון:**

```json
{
  "ip": "8.8.8.8",
  "coordinates": {
    "latitude": 37.386,
    "longitude": -122.084
  },
  "timestamp": "2025-01-12T10:30:00Z"
}
```

### אופן האחסון ב־Redis

Redis ישמש כמאגר Key-Value פשוט:

- **Key**: כתובת ה-IP
- **Value**: JSON string המכיל את נקודות הציון והמידע הנלווה

**דוגמה:**

```
Key: "8.8.8.8"
Value: '{"latitude": 37.386, "longitude": -122.084, "timestamp": "2025-01-12T10:30:00Z"}'
```

### דרישות Validation

כל נתון הנכנס למערכת יעבור ולידציה באמצעות Pydantic:

- כתובת IP חייבת להיות תקינה
- נקודות ציון חייבות להכיל latitude ו-longitude
- latitude חייב להיות בטווח: -90 עד 90
- longitude חייב להיות בטווח: -180 עד 180
- timestamp חייב להיות בפורמט ISO 8601

---

## 6. API Endpoints

### Service A - IP Resolution Service

#### POST /resolve

**תיאור:** קבלת כתובת IP והמרתה לנקודות ציון גאוגרפיות.

**Request Body:**

```json
{
  "ip": "8.8.8.8"
}
```

**Response:**

```json
{
  "status": "success",
  "ip": "8.8.8.8",
  "message": "Coordinates stored successfully"
}
```

**Error Response:**

```json
{
  "status": "error",
  "message": "Invalid IP address"
}
```

---

### Service B - Coordinates Storage Service

#### POST /store

**תיאור:** קבלת נקודות ציון ואחסונן ב-Redis.

**Request Body:**

```json
{
  "ip": "8.8.8.8",
  "coordinates": {
    "latitude": 37.386,
    "longitude": -122.084
  },
  "timestamp": "2025-01-12T10:30:00Z"
}
```

**Response:**

```json
{
  "status": "success",
  "message": "Coordinates stored"
}
```

#### GET /coordinates

**תיאור:** שליפת כל נקודות הציון מהמערכת.

**Response:**

```json
{
  "data": [
    {
      "ip": "8.8.8.8",
      "coordinates": {
        "latitude": 37.386,
        "longitude": -122.084
      },
      "timestamp": "2025-01-12T10:30:00Z"
    },
    {
      "ip": "1.1.1.1",
      "coordinates": {
        "latitude": -37.814,
        "longitude": 144.963
      },
      "timestamp": "2025-01-12T11:00:00Z"
    }
  ],
  "count": 2
}
```

---

## 7. Git Workflow ועבודה בצוות

### Branch Strategy

המערכת תעבוד עם Git Flow מוכוון משימות:

- **main** - ייצור (Production-ready)
- **develop** - פיתוח משולב
- **feature/** - משימות/פיצ'רים ספציפיים

### תהליך עבודה:

1. כל משימה = Branch חדש מ־develop
2. עבודה על ה־Branch האישי
3. Commit תכוף עם הודעות ברורות
4. לאחר סיום --- Push + Pull Request
5. לאחר אישור --- מבצעים Merge

אין לבצע Merge ישיר ללא Pull Request (הכוללת בקרה של חבר הצוות השני, כמפורט להלן).

### Pull Requests ו־Code Review (חובה)

כל Pull Request **חייב לעבור Code Review על ידי מפתח אחר בצוות**.

- אין אישור עצמי
- כל מפתח מבצע Review לקוד של המפתח השני
- רק לאחר Review ואישור מפורש ניתן לבצע Merge

קוד שלא עבר Code Review --- **לא נכנס למערכת**.

### קונבנציה לכתיבת Commit Messages

כל Commit מתאר שינוי אחד ברור.

**מבנה מומלץ:**

```
<type>: <short description>
```

**סוגי Commit נפוצים:**

- `feat:` הוספת פונקציונליות
- `fix:` תיקון באג
- `refactor:` שינוי מבני ללא שינוי התנהגות
- `docs:` תיעוד
- `chore:` תחזוקה או קונפיגורציה

**דוגמאות:**

- `feat: add IP resolution endpoint`
- `fix: handle invalid IP input`
- `refactor: separate service logic from route`
- `docs: update README`

### דגשים חשובים

- Commit אחד = שינוי אחד
- לא דוחפים קוד שבור
- לא מערבבים כמה נושאים באותו Commit
- כל מפתח אחראי על איכות הקוד שהוא מכניס

עבודה לפי סטנדרט זה היא חלק בלתי נפרד מהמשימה ומדמה תהליך עבודה אמיתי בצוותי פיתוח מקצועיים.

---

## 8. מבנה תיקיות וקבצים

מבנה הפרויקט מחולק לפי שירותים, כאשר כל שירות מבודד וברור, ומכיל רק את הקבצים הנדרשים לאחריות שלו.

המבנה נועד:

- לאפשר עבודה מקבילית בין מפתחים
- לשמור על הפרדת אחריות
- להיות ברור גם למי שקורא את הפרויקט בפעם הראשונה

### מבנה כללי של הפרויקט

```
black-coordinates-list/
│
├── service-a/
│   ├── app/
│   │   ├── main.py
│   │   ├── routes.py
│   │   ├── services.py
│   │   ├── Dockerfile
│   │   └── schemas.py
│   │
│   └── requirements.txt
│
├── service-b/
│   ├── app/
│   │   ├── main.py
│   │   ├── routes.py
│   │   ├── storage.py
│   │   ├── Dockerfile
│   │   └── schemas.py
│   │
│   └── requirements.txt
│
├── docker-compose.yml
├── .env.example
└── README.md
```

### הסבר מבנה -- ברמת צוות מקצועי

#### תיקיית שורש (Root)

- `black-coordinates-list/` - מכילה את כל רכיבי המערכת.
- `docker-compose.yml` - מאפשר הרצה מקומית של כלל השירותים וה־Redis.
- `.env.example` - דוגמה למשתני סביבה (לא מכיל סודות).
- `README.md` - תיאור כללי של הפרויקט, הארכיטקטורה ואופן ההרצה.

### Service A -- IP Resolution Service

```
service-a/
├── app/
│   ├── main.py
│   ├── routes.py
│   ├── services.py
│   ├── Dockerfile
│   └── schemas.py
```

**חלוקת אחריות:**

- `main.py` - יצירת אפליקציית FastAPI וחיבור ה־routes.
- `routes.py` - נקודות הקצה (Endpoints) בלבד. ללא לוגיקה מורכבת.
- `services.py` - לוגיקת העבודה:
  - קריאה לשירות החיצוני
  - עיבוד נתונים
  - שליחה ל־Service B
- `schemas.py` - מודלים של בקשות ותשובות (Pydantic).

### Service B -- Coordinates Storage Service

```
service-b/
├── app/
│   ├── main.py
│   ├── routes.py
│   ├── storage.py
│   ├── Dockerfile
│   └── schemas.py
```

**חלוקת אחריות:**

- `main.py` - יצירת אפליקציית FastAPI וחיבור ה־routes.
- `routes.py` - נקודות קצה:
  - קבלת IP עם נקודות ציון
  - שליפת נתונים
- `storage.py` - עבודה מול Redis:
  - חיבור
  - שמירה
  - שליפה
- `schemas.py` - מודלי נתונים (Pydantic).

### קווים מנחים חשובים

- אין קוד לוגיקה בתוך `routes.py`
- כל קובץ אחראי על תפקיד מוגדר וברור
- אין תלות ישירה בין Service A ל־Redis
- התקשורת בין שירותים מתבצעת רק דרך HTTP

---

## 9. דרישות פריסה -- Kubernetes (Minikube)

המערכת **Black Coordinates List** חייבת להיפרס על גבי Kubernetes בסביבת **Minikube מקומית**.

### דרישות כלליות לפריסה:

- כל רכיבי המערכת ירוצו כ־Pods בתוך Kubernetes
- כל שירות Backend יוגדר כך שניתן יהיה להריץ אותו מחדש ללא תלות במופע ספציפי
- רכיב האחסון יוגדר כך שהמידע שבו אינו תלוי בהרצה חד־פעמית
- התקשורת בין רכיבי המערכת תתבצע באמצעות Kubernetes Services בלבד
- לפחות שירות אחד יהיה נגיש מהמחשב הלוקאלי לצורכי בדיקות

הפריסה תתבצע באמצעות קבצי YAML ותנוהל דרך הטרמינל.

### הרחבה אופציונלית

סטודנטים המעוניינים בכך יוכלו, בהמשך, לפרוס את אותה מערכת גם בסביבת Kubernetes מנוהלת (כגון OpenShift), תוך שימוש באותם עקרונות ובקבצי YAML דומים.

---

## 10. שירות חיצוני להמרת IP

### השירות המומלץ: ipapi.co

**Endpoint:**

```
GET https://ipapi.co/{ip}/json/
```

**דוגמה לשימוש:**

```bash
curl https://ipapi.co/8.8.8.8/json/
```

**תשובה לדוגמה:**

```json
{
  "ip": "8.8.8.8",
  "city": "Mountain View",
  "region": "California",
  "country": "US",
  "latitude": 37.386,
  "longitude": -122.084,
  "timezone": "America/Los_Angeles"
}
```

### שירותים חלופיים

- **ip-api.com** - `http://ip-api.com/json/{ip}`
- **ipinfo.io** - `https://ipinfo.io/{ip}/json`
- **freegeoip.app** - `https://freegeoip.app/json/{ip}`

### הערות חשובות

- רוב השירותים מגבילים מספר קריאות לשעה (Rate Limiting)
- יש לטפל בשגיאות רשת ותשובות לא תקינות
- יש לוודא שהשירות החיצוני זמין לפני שליחת בקשות

---

## 11. דרישות טכניות נוספות

### סביבת פיתוח

- Python 3.9+
- FastAPI
- Redis
- Docker & Docker Compose
- Kubernetes (Minikube)
- Git

### ספריות Python נדרשות

**Service A:**
```
fastapi
uvicorn
httpx
pydantic
```

**Service B:**
```
fastapi
uvicorn
redis
pydantic
```

### משתני סביבה

**Service A:**
- `SERVICE_B_URL` - כתובת Service B
- `EXTERNAL_IP_SERVICE_URL` - כתובת שירות המרת IP

**Service B:**
- `REDIS_HOST` - כתובת Redis
- `REDIS_PORT` - פורט Redis (ברירת מחדל: 6379)

### בדיקות (Testing)

מומלץ לכתוב בדיקות יחידה (Unit Tests) עבור:

- ולידציה של נתונים נכנסים
- לוגיקת עיבוד ב־Service A
- פונקציות אחסון ושליפה ב־Service B

---

## 12. הנחיות לביצוע המשימה

### שלב 1: הקמת סביבה

1. יצירת repository ב-Git
2. הגדרת מבנה תיקיות
3. יצירת `.gitignore` מתאים
4. כתיבת `README.md` ראשוני

### שלב 2: פיתוח Service A

1. יצירת FastAPI app בסיסי
2. הוספת endpoint לקבלת IP
3. חיבור לשירות חיצוני
4. עיבוד תשובה
5. שליחה ל-Service B
6. טיפול בשגיאות

### שלב 3: פיתוח Service B

1. יצירת FastAPI app בסיסי
2. חיבור ל-Redis
3. הוספת endpoint לקבלת נקודות ציון
4. שמירה ב-Redis
5. הוספת endpoint לשליפת נתונים
6. ולידציה עם Pydantic

### שלב 4: Docker & Docker Compose

1. כתיבת Dockerfile לכל שירות
2. יצירת docker-compose.yml
3. בדיקת הרצה מקומית
4. תיקון בעיות

### שלב 5: Kubernetes

1. התקנת Minikube
2. כתיבת קבצי YAML:
   - Deployments
   - Services
   - ConfigMaps (אם נדרש)
3. פריסה ל-Minikube
4. בדיקת תקשורת בין שירותים

### שלב 6: בדיקות ותיעוד

1. בדיקות אינטגרציה
2. תיעוד API
3. עדכון README
4. תיעוד תהליך הפריסה

---

## 13. קריטריוני הצלחה

המערכת תיחשב מוצלחת אם:

✅ כל רכיב עובד באופן עצמאי
✅ התקשורת בין רכיבים תקינה
✅ הנתונים נשמרים ונשלפים בהצלחה
✅ הקוד מאורגן ומתועד
✅ המערכת רצה על Kubernetes
✅ עבר Code Review מלא
✅ יש Git history נקי ומובן

---

## 14. משאבים נוספים

### תיעוד רשמי

- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [Redis Documentation](https://redis.io/documentation)
- [Kubernetes Documentation](https://kubernetes.io/docs/)
- [Pydantic Documentation](https://docs.pydantic.dev/)

### מדריכים מומלצים

- FastAPI Tutorial
- Docker Best Practices
- Kubernetes Getting Started
- Git Workflow Guidelines

---

**בהצלחה!**
